# Local (Docker Compose Postgres)
DATABASE_URL="postgresql://app:app@localhost:5432/payables?schema=public"
PORT=3000

# Test (separate database — never point this at your dev DB)
# DATABASE_URL="postgresql://app:app@localhost:5432/payables_test?schema=public"

# Production (Neon) — set via `fly secrets set`, never commit
# DATABASE_URL="postgresql://USER:PASSWORD@HOST.neon.tech/payables?sslmode=require&connection_limit=5"
