# Loaded by `vite build` (production mode). VITE_API_URL is inlined into the
# bundle at build time — it's public (the browser must know it), so committing
# it is fine. Overridable via the build environment.
VITE_API_URL=https://api.stubramp.barbano.uy
